events {}

http {
  server {
    listen 80;

    location / {
      root /usr/share/nginx/html;
      index fallback.html;
    }

    location ~ \.m3u8$ {
      add_header Access-Control-Allow-Origin *;
      root /usr/share/nginx/html;
    }

    location /cam/ {
    root /usr/share/nginx/html;
    add_header Access-Control-Allow-Origin *;
}

    location ~ \.ts$ {
      add_header Access-Control-Allow-Origin *;
      root /usr/share/nginx/html;
    }
  }
}
